# Conda environment for running SnapTools (https://github.com/raphael-group/NAIBR)
name: snaptools
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - conda-forge::python=2.7.6
  - pip:
      - snaptools
